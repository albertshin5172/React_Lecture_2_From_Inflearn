{
  "name": "app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.18.1",
    "dotenv": "^17.2.2",
    "path": "^0.12.7"
  }
}
